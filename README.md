# Demo - How can validate the output Console in .NET

This is a simple demo project that shows how to validate the output of the Console in a .NET application. It demonstrates how to capture. This can be useful for testing purposes, ensuring that the console output is as expected.
